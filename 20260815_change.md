# AutoEnv 2026-08-15 变更总结

## 1. 变更范围

本文汇总从 GitHub `origin/main` 基线 `4f9168b`（`feat: bind labeled resources across environments`）到本地 `main` 的全部修改，并记录合入前检查结果。

本次已有 4 个功能/文档提交：

| 提交 | 内容 |
|---|---|
| `417934a` | 修正脚本交互资源元数据、Web 动态资源输入及契约校验 |
| `a243cd4` | 新增覆盖完整公开接口的环境脚本模板 |
| `f3c6c11` | 新增 Web 与 `adapt_interface.py` 的完整配合说明 |
| `a70ad2e` | 将 Agent 页签改造成可直接操作的 ConPTY 终端 |

在新增本文前，差异共涉及 21 个文件，新增 1,587 行、删除 63 行。

## 2. 需求与实现对应关系

| 需求 | 实现结果 |
|---|---|
| 环境中的网口/串口使用固定标签 | 使用 `1260网口`、`1260串口`、`1712网口`、`1712串口`、`udie1网口`、`udie1串口` 静态目录；环境保存时校验标签、协议类型和唯一性 |
| 脚本明确声明 SSH、Telnet、FTP 交互点 | `register_script(resources=...)` 增加内部名、页面别名、说明、标签和协议元数据；连接注册调用必须复用对应 `resource_label` |
| Web 选择脚本后显示需要填写的资源 | `/api/scripts` 返回 `resources`、`package_inputs`、`parameters`；启动页根据脚本元数据动态渲染连接资源、HDFS 链接和普通参数 |
| 一个脚本可绑定多个环境 | 请求使用 `environments.<脚本资源名>.environment`；每个资源分别选择环境，再按“协议 + 固定标签”查找唯一 IP/连接 |
| 每个 HDFS 包可以单独填写链接 | `packages` 支持包含 `name`、`alias`、`description` 的元数据；Web 每个包生成独立链接输入，填写值转换成 `path_override` |
| 提供全部接口示例 | 新增 `scripts/template.py`，按主机传输、串口、CLI 后置函数和组合脚本四部分展示 |
| 说明 Web 如何触发 Python | 新增 `docs/web_usage.md`，描述原生 Web 框架、按钮/API、请求文件、adapt/interface/registry 调用链和参数绑定 |
| Agent 页签像本地 cmd 一样直接输入 | 删除独立消息框；终端画布直接接收键盘、控制键、文本粘贴和持续刷新输出 |
| 图片复制/拖入后转换成本地路径 | 图片或文件经 `/api/upload` 保存到设置目录，将带双引号的绝对路径写入当前 CLI 光标处，不自动回车 |
| 可以指定 Agent 启动路径 | 设置增加 `agent_cwd`，启动时作为 `PtyProcess.spawn(..., cwd=...)` 的工作目录；不存在的目录会明确报错 |

## 3. 脚本注册与资源契约

### 3.1 `register_script()` 元数据

`autoenv/registry.py` 的 `ScriptDefinition` 新增 `package_inputs`。`register_script()` 现在处理三类 Web 输入：

- `resources`：SSH、Telnet、FTP 连接资源；
- `packages` / `package_inputs`：HDFS 包资源；
- `parameters`：普通脚本参数。

连接资源结构为：

```python
resources=({
    "name": "dut_ssh",
    "alias": "1260 管理网口",
    "description": "用于上传安装包并执行启动命令。",
    "label": "1260网口",
    "protocol": "ssh",
},)
```

其中：

- `name` 是 Python 脚本内部绑定名；
- `alias` 和 `description` 是 Web 页面提示；
- `label` 决定可选择的固定资源标签；
- `protocol` 决定从环境的 SSH、Telnet 或 FTP 分区查找。

HDFS 包结构为：

```python
packages=({
    "name": "A1",
    "alias": "A1 主安装包",
    "description": "从 HDFS 下载的主安装包。",
},)
```

注册表为旧调用保留字符串包名兼容处理，但仓库脚本生成契约要求所有面向 Web 的包使用完整字典元数据。包名和资源内部名都进行重复检查。

### 3.2 生成器和静态契约

`.agents/skills/autoenv-script-generator/` 已同步更新：

- 要求每个 Web 连接资源声明非空 `name`、`alias`、`description`、`label`、`protocol`；
- 要求 SSH/FTP 使用网口标签，Telnet 使用串口标签；
- 要求连接注册调用传入相同的 `resource_label`；
- 要求每个 Web HDFS 包声明非空 `name`、`alias`、`description`；
- 检查资源和包重名；
- 明确 `register_func` 是启动成功后的 CLI 菜单，当前 Web 环境启动页不负责驱动该菜单。

`scripts/example.py` 和 `scripts/download_and_parse_logs.py` 已迁移到新元数据格式。

## 4. 环境注册与 Web 启动

### 4.1 环境资源标签

环境注册页面从静态标签目录下拉选择标签。服务端保存前执行以下校验：

- SSH、FTP 只能使用网口标签；
- Telnet 只能使用串口标签；
- 未知标签被拒绝；
- 同一个环境不能重复使用同一固定标签；
- 环境资源必须有可用的逻辑名、主机地址和对应连接参数。

环境内部逻辑名只用于档案可读性和下拉显示；脚本绑定不要求环境逻辑名与脚本资源名相同。`baud_rate` 当前作为串口档案元数据保存，Telnet TCP 客户端不会自动修改物理串口服务器的波特率。

### 4.2 脚本发现 API

`GET /api/scripts` 除原有脚本名和说明外，现在返回：

```json
{
  "packages": ["A1"],
  "package_inputs": [{"name": "A1", "alias": "A1 主安装包", "description": "..."}],
  "parameters": [{"name": "release_channel", "label": "发布通道"}],
  "resources": [{"name": "dut_ssh", "alias": "管理网口", "description": "...", "label": "1260网口", "protocol": "ssh"}]
}
```

`packages` 继续保留，供旧消费者兼容；新 Web 页面优先使用 `package_inputs`。

### 4.3 动态启动表单

环境启动页不再只有“脚本”和“模式”。选择脚本后会显示：

- 每个 SSH/Telnet/FTP 交互点的别名、说明、协议和固定标签；
- 满足标签要求的“环境 + IP + 环境资源逻辑名”下拉选项；
- 每个 HDFS 包独立的链接输入；
- 脚本声明的普通参数输入。

提交时生成的核心结构为：

```json
{
  "script": "start_demo",
  "mode": "run",
  "environments": {
    "dut_ssh": {"environment": "rack_a"},
    "dut_console": {"environment": "rack_b"}
  },
  "parameters": {
    "packages": {"A1": {"path_override": "/hdfs/build/A1/"}},
    "arguments": {"release_channel": "debug"}
  }
}
```

每个脚本资源独立绑定，因此同一个脚本可以组合多个环境。HDFS 链接留空时仍按 `config.json` 的 `link` / `base_link` 规则解析；填写时使用本次 `path_override`。

## 5. `scripts/template.py` 完整示例

新增模板不会在导入/脚本发现阶段访问远端，但其中地址均为示例地址，不应直接对生产环境运行。

模板分为四个已注册脚本：

1. `template_host_and_transfer`
   - HDFS 下载、普通参数和文件选择器；
   - 单文件/目录提取；
   - SCP、SFTP、独立 FTP 上传；
   - SCP/SFTP 精确或正则远端下载；
   - 下载结果直接作为后续上传选择器；
   - `generate_sh_file()` 与 `S{...}` 已上传文件占位符；
   - SSH `execute()`、`execute_on_output()`、断连预期和业务状态失败转换；
   - `DownloadResult`、`RemoteDownloadResult`、`ExtractResult`、`UploadResult`、`CommandResult` 的预期检查方式。
2. `template_console`
   - SSH 上传与 Telnet `uploaded_files_from`；
   - Telnet shell 模式、命令结果和按关键词发送原始字节。
3. `template_post_start_functions`
   - CLI 专用 `register_func` 循环菜单；
   - 复用主流程连接进行状态检查和日志查询；
   - 明确当前不属于 Web 启动页交互范围。
4. `template_combined`
   - 串行调用多个已注册脚本；
   - 子脚本分别产生独立运行目录、连接和 `ScriptResult`。

模板中的顶部说明同时标出了与 Web 的关联点、结果类型和框架自动管理的生命周期。

## 6. Web 与 Python 调用链文档

新增 `docs/web_usage.md`，完整说明：

- Web 使用浏览器原生 HTML/JavaScript 和 Python 标准库 `ThreadingHTTPServer`，没有 Flask、FastAPI、React 或 Vue 构建链；
- 点击“拉起环境”后，前端调用 `POST /api/run/start`；
- 服务端将结构化请求写入 `webPage/.runtime/request-*.json`；
- 子进程执行 `adapt_interface.py --request ...`；
- `LaunchRequest.from_dict()` → `autoenv.interface.launch()` → `registry.run_script(non_interactive=True)`；
- 已注册环境如何按脚本资源名、协议和标签转换成运行时参数；
- 多环境、HDFS 覆盖、普通参数、输出事件轮询和终止流程；
- Agent ConPTY 使用独立于 adapt interface 的 API 链路。

README、快速入门、详细设计、环境注册指南、Web 接手说明、测试说明和脚本生成 skill 均已同步更新，并互相链接到 `scripts/template.py` 与 `docs/web_usage.md`。

## 7. Agent CLI 页签

### 7.1 终端进程模型

浏览器不能直接嵌入 Windows 原生 cmd 窗口，因此实现采用真实 Windows ConPTY 进程加 Web 终端画布：

```text
终端画布键盘/粘贴事件
    → POST /api/agent/input
    → pywinpty PtyProcess.write()

cmd.exe / codeagent / nga
    → ConPTY 原始终端块
    → GET /api/agent/events
    → Web ANSI 屏幕渲染器
```

主要修改：

- 删除单独的 textarea 和“发送”按钮；
- 点击终端画布后直接输入；
- 键盘请求按顺序排队，避免快速输入导致字符乱序；
- 支持方向键、Home/End、Insert/Delete、翻页、F1-F12、Tab、Escape、Backspace 和 Ctrl 字母组合；
- 终端显示可见光标和焦点/拖放状态；
- 根据页面实际大小计算行列，并通过 `/api/agent/resize` 同步 ConPTY；
- ANSI/OSC 状态跨读取块保存，支持常用光标移动、清屏/清行、插入/删除字符或行、滚屏、保存/恢复光标及窗口标题序列过滤。

### 7.2 文件和图片粘贴/拖放

终端获得焦点后：

- 粘贴普通文本：原样写入 ConPTY；
- 粘贴剪贴板图片：生成文件并调用 `/api/upload`；
- 拖入图片、`.py` 或 `.zip`：调用相同上传接口；
- 服务端使用随机前缀保存文件，限制解码后单文件不超过 48 MiB；
- 前端将返回的绝对路径加双引号后写到当前 CLI 光标处；
- 路径写入后不自动回车，用户可以继续编辑并决定是否执行。

上传只保存文件，不会自动执行 Python 或解压 ZIP。

### 7.3 启动目录

Agent 设置新增：

- `agent_command`：`codeagent`、`nga`、`cmd.exe` 或其他可发现的可执行命令；
- `agent_cwd`：ConPTY 启动目录；
- `upload_dir`：粘贴/拖入文件保存目录。

相对启动路径以项目根目录解析；目录不存在时保存或启动都会失败，不会静默回退。`.cmd`/`.bat` 入口继续通过 `COMSPEC /d /c` 启动。

## 8. 测试与验收

### 8.1 自动测试

合入前执行：

```powershell
python -X utf8 -m compileall -q autoenv scripts webPage
python -X utf8 -m pytest -q
node --check webPage/app.js
git diff --check
```

结果：

- `237 passed`；
- `1 skipped`：当前 Windows 主机不允许创建符号链接；
- 仅出现 Paramiko Blowfish 弃用告警；
- Python 编译、JavaScript 语法和 Git whitespace 检查通过。

新增/扩展测试覆盖：

- 脚本资源与 HDFS 提示元数据；
- 非空别名/说明和重复名称校验；
- 固定标签、协议匹配和环境标签唯一性；
- 多环境资源分别绑定且不影响 HDFS 包参数；
- `/api/scripts` 返回连接/HDFS/普通输入；
- `scripts/template.py` 的 Web 元数据和仓库脚本统一契约；
- ConPTY 原始输出、控制序列、尺寸、输入和 `cwd`；
- Agent 页面没有独立输入框，终端具备键盘、粘贴和拖放入口。

### 8.2 真实浏览器本机验收

使用本地 Web 服务和真实 Windows `cmd.exe` 验证：

- 从指定目录 `F:\codex_project\autoEnv` 启动，cmd 提示符目录正确；
- 直接在终端画布输入并执行命令成功；
- 普通剪贴板文本粘贴成功；
- PNG 剪贴板图片保存后，其绝对路径写入终端且没有自动执行；
- `mode con` 显示页面计算后的 24 行、115 列；
- cmd 的 OSC 窗口标题序列不会泄漏成普通文本；
- 将 `ESC [` 与后续 `2K` 分多个时间片输出时，页面仍正确擦除旧行并显示刷新内容。

浏览器验收生成的临时图片和测试进程均已清理。

## 9. 兼容性、安全和已知限制

- `register_script(packages=("A1",))` 在注册表层仍兼容，但仓库脚本和生成器面向 Web 时要求完整元数据字典；
- 结构化入口仍兼容旧的单个 `environment`，多环境应使用 request JSON 的 `environments`；
- `register_func` 仍为 CLI 后置菜单，Web 启动页没有对应交互控件；
- Agent 页面使用轻量 ANSI 渲染器，不等价于完整 xterm.js；复杂颜色、鼠标模式、IME 和少见控制序列仍有限制；
- Web 默认只监听 `127.0.0.1` 且没有登录鉴权，不应直接暴露到网络；
- 环境密码按现有约定明文保存在默认 Git 忽略目录；
- 文件上传只落盘，不自动执行；
- 离线测试和本机 cmd 验收不能替代真实 SSH、Telnet、FTP、HDFS、串口设备以及真实 `codeagent`/`nga` 的目标网络验收；
- `scripts/download_and_parse_logs.py` 的业务日志块规则仍等待真实日志样例，当前不会猜测实现。

## 10. 文件变更清单

| 分类 | 文件 |
|---|---|
| 注册与契约 | `autoenv/registry.py` |
| 脚本生成 skill | `.agents/skills/autoenv-script-generator/SKILL.md`、`.agents/skills/autoenv-script-generator/scripts/validate_environment_script.py` |
| 环境脚本 | `scripts/example.py`、`scripts/download_and_parse_logs.py`、`scripts/template.py` |
| Web 前端 | `webPage/index.html`、`webPage/app.js`、`webPage/overrides.css` |
| Web 服务 | `webPage/server.py` |
| 文档 | `README.md`、`docs/QUICK_START.md`、`docs/ENVIRONMENT_REGISTRATION_GUIDE.md`、`docs/AutoEnv-Refactor-Detailed-Design.md`、`docs/WEB_ARCHITECTURE_AND_HANDOFF.md`、`docs/web_usage.md`、`webPage/QUICK_START.md` |
| 测试 | `tests/test_runtime_registry.py`、`tests/test_generated_script_contract.py`、`tests/test_interfaces_web_ftp.py`、`tests/README.md` |

## 11. 评审结论

代码、页面协议、示例、生成器约束和相关文档已经对齐，没有发现阻止合入 `main` 的问题。当前修改满足本轮固定资源标签、多环境/多 HDFS 输入、脚本交互点提示、完整模板、Web/adapt 说明及 Agent 终端交互需求；剩余事项均已在“已知限制”中明确标注。
